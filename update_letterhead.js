
const fs = require("fs");
const path = require("path");
const archiver = require("archiver"); // Wait, archiver is an npm module, we might not have it.
// We can use powershell to zip instead. Or just generate the HTML first.

